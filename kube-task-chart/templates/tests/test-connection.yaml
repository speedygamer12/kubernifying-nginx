apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kube-task-chart.fullname" . }}-test-connection"
  labels:
    {{- include "kube-task-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kube-task-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
